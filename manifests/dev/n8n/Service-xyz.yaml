apiVersion: v1
kind: Service
metadata:
  name: xyz
  namespace: n8n
spec:
  loadBalancerClass: tailscale
  ports:
    - port: 5678
      protocol: TCP
      targetPort: 5678
  selector:
    app: n8n
  type: LoadBalancer
