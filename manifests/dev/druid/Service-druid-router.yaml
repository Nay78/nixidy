apiVersion: v1
kind: Service
metadata:
  labels:
    app: druid
    chart: druid-31.0.5
    component: router
    heritage: Helm
    release: druid
  name: druid-router
  namespace: druid
spec:
  ports:
    - name: http
      port: 8888
      protocol: TCP
      targetPort: http
  selector:
    app: druid
    component: router
    release: druid
  type: ClusterIP
