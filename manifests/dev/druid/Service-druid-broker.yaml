apiVersion: v1
kind: Service
metadata:
  labels:
    app: druid
    chart: druid-31.0.5
    component: broker
    heritage: Helm
    release: druid
  name: druid-broker
  namespace: druid
spec:
  ports:
    - name: http
      port: 8082
      protocol: TCP
      targetPort: http
  selector:
    app: druid
    component: broker
    release: druid
  type: ClusterIP
