apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/instance: sops
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: sops-secrets-operator
    app.kubernetes.io/version: 0.17.0
    helm.sh/chart: sops-secrets-operator-0.23.0
  name: sops-sops-secrets-operator
  namespace: sops
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: sops
      app.kubernetes.io/name: sops-secrets-operator
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: sops-secrets-operator
      labels:
        app.kubernetes.io/instance: sops
        app.kubernetes.io/name: sops-secrets-operator
        control-plane: controller-sops-secrets-operator
    spec:
      containers:
        - args:
            - -health-probe-bind-address=:8081
            - -leader-elect
            - -requeue-decrypt-after=5
            - -zap-devel=false
            - -zap-encoder=json
            - -zap-log-level=info
            - -zap-stacktrace-level=error
            - -zap-time-encoding=iso8601
          command:
            - /usr/local/bin/manager
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          image: isindir/sops-secrets-operator:0.17.0
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          name: sops-secrets-operator
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources: {}
          volumeMounts:
            - mountPath: /etc/sops-age-private-key
              name: sops-age-private-key
              readOnly: true
      serviceAccountName: sops-sops-secrets-operator
      volumes:
        - name: sops-age-private-key
          secret:
            secretName: sops-age-private-key
