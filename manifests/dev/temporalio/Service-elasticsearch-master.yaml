apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: elasticsearch-master
    chart: elasticsearch
    heritage: Helm
    release: temporalio
  name: elasticsearch-master
  namespace: temporal
spec:
  ports:
    - name: http
      port: 9200
      protocol: TCP
    - name: transport
      port: 9300
      protocol: TCP
  publishNotReadyAddresses: false
  selector:
    app: elasticsearch-master
    chart: elasticsearch
    release: temporalio
  type: ClusterIP
